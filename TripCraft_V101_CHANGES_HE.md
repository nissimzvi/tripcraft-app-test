# TripCraft V101 — שינויים ובדיקות

- שתי חבילות נפרדות: Mobile ו־Admin Desktop.
- Admin ללא OTP: כניסה ראשונית "ניסים צבי" / `1234`, והחלפת סיסמה חובה.
- טבלת משתמשי ניהול עם הוספה, עריכה, חסימה, שינוי סיסמה ומחיקה.
- Admin חסום במובייל ואינו נכלל בחבילת המובייל.
- OTP למשתמשי המובייל שונה ל־6 ספרות בכל שדות הקלט והאימות.
- כפתור "בנה טיול" מציג את התוצאה מיד; שמירת הענן מתבצעת ברקע ואינה יכולה לעכב את הצגת הטיול.
- נוסף אימות ישיר של שם טיול, יעד ותאריכים לפני הבנייה.
- טיול חדש מאפס טיוטה קודמת וערכים ישנים, לרבות TYROL.
- הלוגו שסופק נמצא ב־`assets/tripcraft-logo.png`, ונוספה גרסת cache `?v=101` כדי למנוע הצגת לוגו ישן מהמטמון.
- הודעת כשל SMTP/Confirmation Email מפנה במפורש ל־Supabase Authentication → Email/SMTP → Logs.

## תלות חיצונית שעדיין דורשת הגדרה

הודעת `Error sending confirmation email` מגיעה מ־Supabase ולא מהטופס. יש לבדוק בפרויקט Supabase את ספק ה־SMTP, מגבלת השליחה וה־Auth Logs. ללא שליחת מייל תקינה אי אפשר להשלים רישום משתמש חדש באמצעות OTP אמיתי.
