# TripCraft V99 — אפיון מלא ודוח ביצוע

בסיס העבודה: תג `v97-qa` (נקודת ההחזרה V97). V97 לא שונתה; התיקונים נמצאים ב־V99.

מקרא: **בוצע** = קיים בקוד V99; **חלקי** = קיים אך אינו מממש את מלוא דרישת הפיילוט; **חסר/לא אומת** = אין להציגו כמוכן.

## 1. טיסות אינן חובה + ישראל

האפיון: ניתן לבנות טיול ללא טיסות; במקרה זה נשאל “איפה מטיילים?”, לרבות ישראל. אם קיימות טיסות הן משפיעות על היום הראשון והאחרון.

**בוצע ב־V99:** שדות הנחיתה והחזרה סומנו כאופציונליים, בדיקת החובה הוסרה, והמסלול אינו יוצר נקודות שדה ריקות. שדה היעד אומר במפורש “איפה מטיילים?” וכולל ישראל. כשיש טיסות הן משובצות בקצוות המסלול. **בדיקה:** בדיקת קוד ונתיב ללא שדות טיסה; לא בוצעה בדיקת דפדפן מלאה.

## 2. דף הבית

האפיון: להסיר “הטיולים שלי” מדף הבית; להציגו רק לאחר התחברות ורק כשיש טיולים.

**בוצע:** קישור “הטיולים שלי” הוסר מדף הבית. לאחר OTP משתמש עם טיולים מועבר למסך נפרד; ללא טיולים מועבר לטיול חדש.

## 3. מנוע התכנון

האפיון: שימוש בכל נתוני השאלון, בלי המצאת העדפות, מסלול איכותי והגיוני גאוגרפית ובפורמט אחיד.

**חלקי:** הנתונים נשמרים בטיוטה ונעשה בהם שימוש בתבנית המסלול. המנוע הנוכחי הוא מנוע כללים/דמו ואינו מנוע AI מחובר שמסוגל להבטיח איכות גאוגרפית מלאה. אין לאשר סעיף זה כמושלם לפני חיבור ספק AI ובדיקות מסלולים אמיתיות.

## 4. הצגת ימי הטיול

האפיון: כרטיס צבעוני לכל יום, פתיחה, עריכה/תכנון מחדש של יום בודד ושימוש בפוסטים/קבצים.

**חלקי:** קיימים כרטיסי ימים, פתיחה ועריכת יום/כותרת ושדה המלצות. אין העלאת קובץ מלאה המחוברת לתכנון מחדש של יום באמצעות AI.

## 5. מבנה קבוע של יום

האפיון: מסלול Google Maps מלא; ציר שעות; שם, תיאור, משך וזמן מעבר; Waze/Maps במצב נהיגה או הליכה.

**חלקי:** קיימים קישורי מסלול יומי, Google Maps ו־Waze ונקודות מסודרות. כל הנתונים אינם מופיעים עדיין באופן אחיד בכל נקודה, והבחירה Walking/Driving אינה מאומתת לכל תרחיש.

## 6. עלויות יומיות

האפיון: מחיר לפי מבוגר/ילד/Senior וסיכום יומי, ללא המצאת מחיר.

**חלקי:** מוצגת הערכת תקציב לפי הרכב נוסעים. המחירים אינם מגיעים ממקור מחירים חי ואמין ואין פירוט Senior מלא; לכן אין לאשר כעלות אמת.

## 7. חלופה ליום גשום

האפיון: חלופה אזורית מלאה באותו פורמט.

**חסר:** טרם קיים מנגנון מלא ואמין לחלופת גשם לכל יום.

## 8. שמירה וקישור קבוע

האפיון: הטיול מלא ופתוח בפיילוט, Trip ID, קישור קבוע וכפתור “העתק קישור”.

**בוצע בקוד:** אין נעילת סוף מסלול; לאחר בנייה מתבצעת שמירה, נוצר מזהה וקישור, וכפתור ההעתקה מאשר “הקישור הועתק”. התמדה בין מכשירים תלויה בטבלת Supabase `tripcraft_trips` ובהרשאות התקינות שלה.

## 9. הרשמה / התחברות / OTP

האפיון: בדיקת מייל, משתמש חדש/קיים, מסך OTP נפרד, קוד נכון/שגוי/פג תוקף/שליחה מחדש ומניעת כניסה ללא קוד.

**בוצע בקוד:** מסך OTP נפרד, שליחה ואימות דרך Supabase Auth, הודעות שגיאה ושליחה מחדש; אין מעבר למסך מוגן ללא Session. **לא אומת בפועל:** קבלה לתיבת מייל, תפוגה ולוגים אצל ספק — דורש חשבון וסביבת ספק זמינים.

## 10. לאחר Login + OTP

האפיון: בעל טיולים עובר ל“הטיולים שלי”; חסר טיולים עובר לטיול חדש.

**בוצע ב־V99:** לאחר אימות נטענת רשימת הטיולים. רשימה לא ריקה פותחת `account`; רשימה ריקה מאפסת את הטופס ופותחת `planner`.

## 11. “הטיולים שלי”

האפיון: מסך נפרד, חדש ראשון, שם/יעד/תאריכים/קישור/פתיחה/עריכה וכפתור טיול חדש.

**בוצע:** הרשימה נטענת לפי `updated_at` יורד ומציגה שם, יעד, תאריכים, קישור וכפתור פתיחה/עריכה. נוסף כפתור “בניית טיול חדש”.

## 12. עריכת טיול קיים

האפיון: טעינת כל הנתונים המקוריים לשאלון ותכנון מחדש מלא או של יום יחיד.

**בוצע ברמת הטופס:** פתיחת טיול משחזרת את הטיוטה והשדות המרכזיים, כולל יעד, טיסות, תאריכים, נוסעים, תחבורה, קצב, העדפות ולינה. **חלקי:** תכנון AI מחדש של יום יחיד אינו מחובר לספק AI אמיתי.

## 13. Admin

האפיון: Admin מוגן עם Settings ו־Users; משתמש רגיל חסום.

**חלקי:** בדיקת הרשאה משתמשת ב־`app_metadata.role === admin`, ולא בדגל שניתן לערוך בדפדפן. מסכי הניהול הקיימים עדיין כוללים רכיבי דמו ואינם Admin production מלא.

## 14. Admin — Settings ואינטגרציות

האפיון: מרכז לכל ההגדרות והאינטגרציות, סטטוס, סוג API, מסמכים, Dashboard, Test Connection וזמן בדיקה; Secrets בשרת בלבד. Booking צריך E2E וכפתור Dashboard; קישורים גם ל־Payoneer, OTP/Email, Google, AI וכל ספק.

**חלקי/לא מאושר:** קיימות הגדרות דמו ל־Booking/AI, אך אין קטלוג production מלא, בדיקת חיבור שרתית לכל ספק או הוכחת Click/Conversion של Booking. אין להקליד Secret במסך לקוח. Booking, Payoneer, ספק המייל/OTP, Google Cloud וספק AI דורשים כתובות Dashboard והרשאות אמיתיות לפני השלמה.

## 15. Admin — Users

האפיון: רשימת משתמשים מרכזית וטיוליהם עם קישורים.

**חלקי:** קיים מסך משתמשים/טיולים דמואי. גישה מלאה ל־Auth Users מחייבת endpoint שרתי עם Service Role ואינה יכולה להתבצע בבטחה מ־HTML/JavaScript.

## 16. תשלומים — הכנה

האפיון: בעתיד פרטי עסקה וארבע ספרות אחרונות בלבד.

**בוצע כהכנת דמו בלבד:** נשמרות ארבע ספרות אחרונות ולא מספר מלא. אין ספק סליקה אמיתי.

## 17. הזרימה הסופית

האפיון: Home → Login/Registration → Email → OTP → My Trips/New Trip → שאלון → טיסות אופציונליות → Generate → Save → Trip ID/Link → Copy.

**בוצע מבנית ב־V99:** כולל מסך הביניים המבוקש. פתיחת טיול קיים משחזרת נתונים; טיול חדש מוחק את `tripcraft_planner_draft_v57`, מאפס מזהה ותוצאה ומחזיר רק ברירות מחדל. כך TYROL מטיול קודם אינו נשמר לטיול חדש.

## 18. בדיקות חובה

האפיון: E2E למשתמש חדש וקיים, טיסות/ללא טיסות/ישראל, Maps/Waze/Walking, גשם, עלויות, יום יחיד, העלאה, Admin, DB וכל מצבי OTP; וכן Booking tracking, OTP logs, Payoneer וכל API.

**בוצע:** בדיקות תחביר JavaScript, בדיקות עקביות קבצים/גרסה, בדיקת הפניות לנכסים וסריקת קוד לזרימות האיפוס/טעינה/שמירה. **לא בוצע ולכן לא נטען שבוצע:** OTP למייל אמיתי, Booking Partner clicks/conversions, Payoneer, ספקי API, בדיקת דפדפן פיזי iPhone/Android ובדיקת E2E מול production. אלה דורשים Credentials, חשבונות ספק וסביבת פריסה נגישה.

## 19. הנחיה קריטית

האפיון: לא לשנות עיצוב שלא נדרש, לא להסיר פונקציות, לא להתחיל מחדש, Responsive, ולהשלים קודם את זרימת הליבה; כשחסר מידע לא לנחש.

**בוצע:** העבודה יצאה ישירות מ־V97 ונשמרה כ־V99 נפרדת. העיצוב נשמר, הלוגו שסופק שובץ בכותרת, נוספו התאמות Safe Area למובייל, והזרימה תוקנה בלי לשנות את V97. סעיפים התלויים בחשבונות חיצוניים מסומנים במפורש כלא מאומתים.

## תיקוני הבאגים שנמסרו לאחר האפיון

1. **TYROL נשמר:** תוקן — טיול חדש מנקה טיוטה, מזהה, תוצאה וכל ערכי המשתמש; נשארות רק ברירות המחדל.
2. **מסך ביניים אחרי OTP:** תוקן — רשימה עם שם טיול, יעד, תאריכים, קישור, פתיחה/עריכה וטיול חדש.
3. **פתיחת קיים:** תוקן — הנתונים השמורים נטענים לטופס לפני העריכה.
4. **חדש נקי:** תוקן — כולל איפוס קבוצות גיל ל־2 מבוגרים ו־0 ביתר.
5. **“בנה טיול” לא עובד:** תוקנה חסימת הטיסות, נוספה הגנה מלחיצה כפולה, שמירה אוטומטית ושגיאה גלויה במקרה כשל. נדרשת עדיין בדיקת דפדפן E2E בסביבת היעד.
6. **לוגו:** שובץ `assets/tripcraft-logo.png` בכותרת ובמטמון האפליקציה.
7. **מובייל:** אותו קוד Responsive מיועד ל־iPhone ולאנדרואיד; אין שתי אפליקציות Native נפרדות בחבילה.

## מידע שחסר להשלמת סעיפי Production

- גישת בדיקה לחשבון Booking Affiliate וה־Affiliate/Tracking ID המדויק.
- קישור/חשבון Payoneer והגדרת השימוש בפועל (Payoneer אינו Payment Gateway כללי בכל תרחיש).
- זיהוי ספק OTP/Email והרשאה ללוגים.
- פרויקט Google Cloud/Maps וה־API הפעילים.
- ספק AI, endpoint, מודל ומנגנון Billing/Usage.
- Backend מאובטח ל־Admin Users ולבדיקות Connection; אין להעביר Service Role או Secrets לקוד הלקוח.

## עדכון V100 — הפרדה מוחלטת בין מובייל לניהול

- נוצרה חבילת משתמשים `TripCraft_Mobile_V100`: אין בה מסך Admin, אין קישור Admin, ונתיב `#admin` אינו נתיב מוכר ומוחזר לדף הבית.
- נוצרה חבילת ניהול נפרדת `TripCraft_Admin_Desktop_V100`: היא מיועדת למחשב בלבד וחוסמת תצוגה במסכים קטנים ובמכשירי מגע ניידים.
- הכניסה לניהול דורשת Email OTP וגם `app_metadata.role === admin` ב־Supabase. OTP תקין של משתמש רגיל אינו מספיק.
- באדמין קיימות שלוש לשוניות: Settings/Integrations, Users/Trips, Security/Tests.
- Settings כולל Supabase Database, OTP/Email, Booking Affiliate, Google Maps, Waze, AI Provider, Payoneer ו־GitHub Pages; לכל שירות מוצגים מטרה, סוג חיבור, סטטוס, מסמכים, מערכת ניהול, Test Connection וזמן בדיקה.
- Secrets אינם מוצגים ואין באדמין שדה להזנת API Key או סיסמה.
- Users מציג משתמשים שנגזרים מהטיולים המורשים לקריאה בטבלת `tripcraft_trips`. הצגת כל משתמשי Supabase Auth עדיין מחייבת Backend מאובטח עם Service Role, ולכן אינה מזויפת בצד הלקוח.
- Booking מוצג כ־Not Connected כל עוד `bookingAffiliateTemplate` ריק; אין הצהרה על Tracking/Conversions בלי בדיקת חשבון Partner אמיתית.
