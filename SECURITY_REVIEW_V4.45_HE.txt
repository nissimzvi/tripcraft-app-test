TripCraft V4.46 — בדיקת אבטחה סטטית

1. אין לשמור OpenAI/Booking API Secret, סיסמאות או נתוני כרטיס בקוד GitHub או localStorage.
2. בגרסת GitHub נשמרים רק Partner ID וכתובת Backend לא סודיים.
3. נוספה מדיניות CSP בסיסית, object-src none, base-uri self, frame-ancestors none ו-referrer policy.
4. העלאת קבצים מוגבלת ל-xlsx/xls/csv/docx/pdf ובגודל עד 12MB.
5. CSV חייב להיות מופרד בפסיקים; Word/PDF חייבים להיות טבלאיים לפי 5 הטורים.
6. פרטי לקוחות, OTP, תשלומים, הזמנות ומפתחות API חייבים לעבור ל-Backend ומסד נתונים לפני שימוש מסחרי.
7. GitHub Pages הוא Frontend סטטי בלבד ואינו שכבת אבטחה להרשאות Admin.
8. מספר כרטיס מלא/CVV אינם נשמרים במערכת; בפרודקשן יש להשתמש ב-tokenization של ספק הסליקה.
