# TripCraft Mobile V115

V115 נבנתה מ־V114 המאושרת ומטפלת רק בשמירה, עדכון, יציאה ללא שמירה, שינוי באמצעות AI ופתיחת פירוט יום.

## תיקוני V115

- שמירת טיול חדש מתבצעת ישירות מול חשבון המשתמש ומקבלת אישור מבסיס הנתונים לפני הצגת הצלחה.
- לאחר שמירה מוצלחת חוזרים אוטומטית ל־"החשבון שלי" והטיול מופיע מיד ברשימת "הטיולים שלי".
- טיול קיים נפתח במצב עדכון ושומר על אותו Trip ID ועל אותו קישור אישי.
- במצב עדכון מוצגים "שמור עדכון" ו־"יציאה ללא שמירה".
- יציאה ללא שמירה מבטלת את השינויים בזיכרון, אינה מוחקת את הטיול המקורי וחוזרת לחשבון.
- "פתח פירוט יום" משתמש בנתיב לחיצה ישיר ופותח את היום המדויק גם במגע באייפון.
- כפתורי "שלח" ברמת הטיול וברמת היום מפעילים שינוי מובנה, מסמנים את הטיול כטרם נשמר ומחייבים "שמור עדכון" לשמירה סופית.
- נוסף נתיב AI מאובטח דרך Supabase Edge Function, ללא מפתח סודי בקוד האתר.
- כל פעולות V114 האחרות נשמרו ללא שינוי.

## אבטחת AI

הקובץ `supabase/functions/tripcraft-ai/index.ts` הוא פונקציית השרת המאובטחת. הוראות הפריסה נמצאות ב־`SUPABASE_AI_V115.md`. לפני פריסת הפונקציה המערכת יודעת לבצע מקומית בקשות בסיסיות ומוגדרות בלי להציג הצלחה כוזבת.

## בדיקות קבלה

- שמירת טיול חדש וחזרה לחשבון.
- פתיחה ועדכון של אותו Trip ID ללא כפילות.
- יציאה ללא שמירה וחזרה לחשבון.
- פתיחת פירוט של ימים שונים.
- שינוי מקומי ושינוי דרך תשובת AI תקינה.
- כשל שמירה וכשל AI ללא איבוד הטיול.
- רגרסיה של חשבון, תאריכים, מלונות, שפה, לוגו, קישורים ותצוגת מובייל.
